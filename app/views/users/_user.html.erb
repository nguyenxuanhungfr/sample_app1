<li>
  <%= gravatar_for user, size: Settings.user_model.max_length_name %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("delete"), user, method: :delete, data: { confirm: t("sure") } %>
  <% end %>
</li>
